# สรุปโครงสร้างและการทำความเข้าใจไฟล์ `testLeague.xlsx`

ไฟล์ `testLeague.xlsx` เป็นไฟล์การกำหนดค่าแบบสเปรดชีต (Spreadsheet Configuration) ที่ใช้ร่วมกับระบบ **Football Scoreboard Controller** เพื่อโหลดข้อมูลแมตช์การแข่งขัน ชื่อทีม สีประจำทีม และโครงสร้างฐานข้อมูล Firebase Realtime Database เข้ามาใช้งานในคอนโทรลเลอร์

จากการตรวจสอบและวิเคราะห์ข้อมูลผ่านสคริปต์ พบว่าตัวไฟล์ประกอบด้วย **4 ชีตหลัก** ดังนี้:

---

## 1. ชีต: `Matching` (ตารางจับคู่แข่งขัน)
ใช้ในการเก็บรายชื่อคู่แข่งขันและรายละเอียดเบื้องต้นของแต่ละแมตช์ โดยระบบคอนโทรลเลอร์จะอ้างอิงจาก **Match ID** เพื่อดึงคู่แข่งขันไปแสดงผลและควบคุม

* **จำนวนแถวทั้งหมด:** 51 แถว (แถวที่ 1 เป็น Header และแถวที่ 2-51 เป็นข้อมูลแมตช์ 1 ถึง 50)
* **โครงสร้างคอลัมน์:**
  * `Match`: รหัสแมตช์การแข่งขัน (Match ID)
  * `Team_A`: ชื่อทีมเหย้า (Home Team)
  * `Team_B`: ชื่อทีมเยือน (Away Team)
  * `Label_1` ถึง `Label_5`: ป้ายข้อมูลเพิ่มเติม เช่น รอบการแข่งขัน (Round), สัปดาห์ (Week), หรือ สนามแข่งขัน (Field)
* **ข้อมูลแมตช์ที่ระบุไว้จริง (แถวที่ 2 - 6):**
  | Match ID | Team A (เหย้า) | Team B (เยือน) | Label_1 (ตัวอย่าง) |
  | :---: | :--- | :--- | :--- |
  | **1** | YEDPED FC x WHITE DEVIL | ซ้อแนน X ผู้ใหญ่จ็อบ By.แก๊งค์4พ่อบ้าน | Test |
  | **2** | SMURF FC BY.บังนัท | YEDPED FC x WHITE DEVIL | Test |
  | **3** | MPK-มาบปลาเค้า | ซ้อแนน X ผู้ใหญ่จ็อบ By.แก๊งค์4พ่อบ้าน | Test |
  | **4** | คนละร้อย FC X สามแม่ครัว | ทนงทวย X บ้านบางตะบูน | Test |
  | **5** | ทีมเดอะแก๊ง & CTลิขิต | พวกพี่เสือเก่า | Test |
  
  *หมายเหตุ: แมตช์ที่ 6 ถึง 50 มีการใส่ตัวเลข Match ID ล่วงหน้าไว้แล้วในคอลัมน์แรก แต่คอลัมน์อื่น ๆ ยังว่างอยู่เพื่อรอการกรอกข้อมูลในอนาคต*

---

## 2. ชีต: `Team` (ข้อมูลสโมสรและทีม)
ใช้สำหรับขึ้นทะเบียนทีมฟุตบอลและเก็บข้อมูลการออกแบบ เช่น สีประจำทีม (Color) และโลโก้ (Logo)

* **จำนวนแถวทั้งหมด:** 16 แถว (ใช้จริง 10 ทีม)
* **โครงสร้างคอลัมน์:**
  * `TEAM`: ชื่อทีมฟุตบอล
  * `Color_1` & `Color_2`: สีหลักและสีรองของทีม (ในไฟล์ตัวอย่างนี้ยังไม่ได้ระบุค่าสี Hex ไว้ล่วงหน้า)
  * `หมายเหตุ.`: คำแนะนำผู้กรอกข้อมูล
* **รายชื่อทีมฟุตบอลที่ลงทะเบียนไว้:**
  1. PROMMA PB 100
  2. CPF เพชรบุรี
  3. SMURF FC BY.บังนัท
  4. YEDPED FC x WHITE DEVIL
  5. MPK-มาบปลาเค้า
  6. ซ้อแนน X ผู้ใหญ่จ็อบ By.แก๊งค์4พ่อบ้าน
  7. คนละร้อย FC X สามแม่ครัว
  8. ทนงทวย X บ้านบางตะบูน
  9. ทีมเดอะแก๊ง & CTลิขิต
  10. พวกพี่เสือเก่า
* **ข้อกำหนดสำคัญในคอลัมน์หมายเหตุ:**
  > [!IMPORTANT]
  > ในชีตระบุคำเตือน: **"ตั้งชื่อ logo ให้ตรงกับชื่อทีม เช่น [เมืองไทยแคปปิตอล.png]"**
  > ระบบ Scoreboard จะพยายามโหลดรูปโลโก้ที่มีชื่อไฟล์ตรงกับชื่อทีมในโฟลเดอร์โลโก้ที่ผู้ใช้ตั้งค่าไว้ (ค่าเริ่มต้นคือ `C:/OBSAssets/logos`) หากตั้งชื่อไฟล์รูปตรงกันระบบจะแสดงผลโลโก้อัตโนมัติ

---

## 3. ชีต: `FirebaseRealtimeDatabase` (การตั้งค่าฐานข้อมูลภายนอก)
ชีตนี้มีความสำคัญสูงสุดในการส่งผ่านข้อมูลจากคอนโทรลเลอร์ไปยัง OBS (ผ่าน Firebase Realtime Database เพื่อควบคุมหน้าจอสตรีมมิ่ง) ระบบจะอ่านค่าคอนฟิกนี้เพื่อสร้างปุ่มบันทึกแมตช์ลงในฐานข้อมูลที่ต้องการ

* **จำนวนแถวทั้งหมด:** 27 แถว
* **การแบ่งกลุ่มโครงสร้าง:** แบ่งออกเป็น 2 บล็อก (บล็อกใช้งานจริง และบล็อกตัวอย่าง)

### บล็อกที่ 1: การกำหนดค่าจริงของลีกปัจจุบัน (`CREATOR LEAGUE SS3`)
* **ชื่อลีก (League Name):** CREATOR LEAGUE SS3
* **ข้อมูลเชื่อมต่อ Firebase:**
  * `apiKey`: `AIzaSyAdID09MPyVviwg9VDshY4e4TLU91fHb-0`
  * `authDomain`: `creatorleague-6ddf9.firebaseapp.com`
  * `databaseURL`: `https://creatorleague-6ddf9-default-rtdb.firebaseio.com`
  * `projectId`: `creatorleague-6ddf9`
  * `storageBucket`: `creatorleague-6ddf9.firebasestorage.app`
  * `messagingSenderId`: `797703682911`
  * `appId`: `1:797703682911:web:06a1dc9a3108e458d094ea`
  * `measurementId`: `G-SC448ZV16Y`

### บล็อกที่ 2: การกำหนดค่าตัวอย่าง (`exam // Firebase config`)
* **กลุ่มแถวที่ 19-27:** แสดงข้อมูลคอนฟิกจำลองสำหรับฐานข้อมูลตัวโครงการทดสอบ
  * `projectId`: `general-21c81`
  * `databaseURL`: `https://general-21c81-default-rtdb.firebaseio.com`

---

## 4. ชีต: `DetailMatch` (รายละเอียดแมตช์เชิงลึก)
* **จำนวนแถวทั้งหมด:** 0 แถว
* **การใช้งาน:** ชีตนี้ว่างเปล่าในไฟล์ทดสอบ อาจใช้สำหรับเก็บข้อมูลเชิงสถิติ (เช่น ผู้ทำประตู ใบเหลือง ใบแดง) ในเวอร์ชันถัดไป

---

## การเชื่อมโยงกับโค้ดคอนโทรลเลอร์ (`main.js` & `overlay.js`)

1. **การโหลดข้อมูล (Excel Parsing):** เมื่อผู้ใช้อัปโหลดไฟล์นี้ผ่านปุ่ม Excel บนบราวเซอร์ ฟังก์ชัน `handleExcel()` ใน `main.js` จะดึงข้อมูลชีตจับคู่แมตช์ด้วยการสแกนหาชื่อชีตที่ไม่ได้ชื่อ `FirebaseRealtimeDatabase` หรือชีตที่มีชื่อใกล้เคียงกับแมตช์เพื่อใช้เป็นข้อมูลของคู่แข่งขันหลัก
2. **การ Mapping คอลัมน์แบบยืดหยุ่น (Column Mapping):** โค้ดมีการใช้ `mergeExcelMapping` เพื่อจับคู่หัวตาราง เช่น `Team_A` หรือ `TeamA` เข้าหาค่ามาตรฐานของโปรแกรม และหากผู้ใช้ต้องการเชื่อมฟิลด์ต่างออกไป ก็สามารถไปที่หน้าต่าง **Excel Mapping** บนหน้าเว็บเพื่อทำการแก้ไขได้
3. **การบันทึกคะแนน Realtime:** หากมีแถวข้อมูลฐานข้อมูลในชีต `FirebaseRealtimeDatabase` ระบบจะประมวลผลผ่าน `parseFirebaseSaveTargets` แล้วสร้างปุ่มที่ระบุคำว่า **"บันทึกข้อมูลแมทต์ CREATOR LEAGUE SS3"** ขึ้นมาบนหน้าจอด้านล่างทันที เมื่อกดปุ่ม โปรแกรมจะส่งข้อมูลคะแนน ชื่อทีม และป้ายกำกับของแมตช์นั้นเข้าสู่ Realtime Database เพื่อให้บราวเซอร์สำหรับสตรีมมิ่ง (`overlay.html`) อัปเดตตารางคะแนน ผลแพ้ชนะ และแถบเลื่อน (Ticker) ในหน้าจอ OBS ของผู้ถ่ายทอดสดโดยอัตโนมัติ
